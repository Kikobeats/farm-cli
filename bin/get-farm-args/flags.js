'use strict'

module.exports = {
  delayBetweenWorkers: {
    alias: ['d', 'delay']
  },
  maxConcurrentWorkers: {
    alias: ['n', 'cores']
  },
  maxConcurrentCallsPerWorker: {
    alias: ['w', 'workers']
  },
  maxRetries: {
    alias: ['r', 'retries']
  },
  autoStart: {
    alias: ['autostart', 'auto']
  }
}
